# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# Note: api_key is encrypted, so we can't use fixtures here easily.
# Tests create credentials dynamically instead.